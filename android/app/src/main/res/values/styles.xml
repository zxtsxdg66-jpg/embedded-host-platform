<?xml version="1.0" encoding="utf-8"?>
<!-- 复用型样式：卡片、区块标题、正文小字。避免在每个布局里重复十几行属性。 -->
<resources>

    <!-- 卡片：零阴影 + 1dp 描边。用描边而不是投影划分层次，观感更"工程" -->
    <style name="Widget.EnvMonitor.Card" parent="Widget.Material3.CardView.Outlined">
        <item name="cardBackgroundColor">@color/panel_aux</item>
        <item name="cardCornerRadius">@dimen/card_radius</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeColor">@color/stroke_card</item>
        <item name="strokeWidth">@dimen/stroke_width</item>
    </style>

    <!-- 指标卡：三个核心读数所在，透明度最低，背景图不得干扰这里 -->
    <style name="Widget.EnvMonitor.Card.Metric">
        <item name="cardBackgroundColor">@color/panel_metric</item>
    </style>

    <style name="TextAppearance.EnvMonitor.SectionTitle" parent="">
        <item name="android:textSize">@dimen/text_metric_name</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="TextAppearance.EnvMonitor.Caption" parent="">
        <item name="android:textSize">@dimen/text_caption</item>
        <item name="android:textColor">@color/text_tertiary</item>
    </style>


    <!-- 历史页的通道切换：选中态靠 selected 状态切换背景，不额外引入控件库 -->
    <style name="HistoryChip">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/bg_pill_selectable</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:paddingStart">14dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingEnd">14dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:textColor">@color/history_chip_text</item>
        <item name="android:textSize">@dimen/text_body</item>
    </style>
</resources>
