/**
 ****************************************************************************************************
 * @file        board_uart_msp.h
 * @brief       集中实现 HAL_UART_MspInit()——HAL 库要求每个工程只能有一份该函数的定义
 *              （它是弱符号的默认实现，链接期只允许存在一份强符号覆盖），因此所有用到
 *              的 UART 外设（USART1/USART3）的引脚与时钟初始化都汇总在这一个文件里，
 *              而不是分散在 pc_link.c / noise_usart.c 各自文件中——避免重复定义冲突。
 *
 * 引脚分配依据 docs/hardware.md：
 *   USART1: PA9(TX)/PA10(RX)，经开发板 P10 跳线接板载 CH340C，用于 PC 通信
 *   USART3: PB10(TX)/PB11(RX)，经 P2 排针原始引出，用于噪声传感器 Modbus RTU 通信
 ****************************************************************************************************
 */
#ifndef __BOARD_UART_MSP_H
#define __BOARD_UART_MSP_H

/* 本文件不对外暴露函数——HAL_UART_MspInit() 由 HAL_UART_Init() 自动回调，
 * 无需任何模块显式调用，此头文件仅用于说明本模块的存在与职责，供其它文件
 * 的注释引用，避免遗忘。 */

#endif
