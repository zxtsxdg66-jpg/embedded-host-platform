# 参与开发

## 准备

```bash
pip install -e ".[dev]"
```

Python 3.10 及以上。写代码按 3.10 的语法上限来（`ruff` 与 `mypy` 都锁在 3.10），不要用更新的特性。

**不要顺手加依赖。** 新增运行期依赖要在 PR 里单独说明理由；能用标准库做的用标准库
（历史库用 `sqlite3`、CRC 用 `zlib`，都是这么定的）。

## 提交前必须通过

```bash
pytest                          # 全部测试，不需要硬件、网络或语言模型
ruff check src tests scripts    # 行宽 88；规则集 E,W,F,I,B,C4,UP
mypy src                        # 公开函数必须有完整类型标注
```

动了对应部分时另跑：

| 改了什么 | 跑什么 |
| --- | --- |
| 固件工程 | `python scripts/check_keil_project.py` |
| 文档结构或链接 | `python scripts/check_doc_links.py` |
| 测试数、源文件数等规模数字 | `python scripts/check_doc_numbers.py` |
| 公开的类名、函数名 | `python scripts/check_doc_identifiers.py`（输出需人过目） |

三项里任何一项不过，改动就不算完成。不要为了让检查通过而放宽检查本身。

## 工作方式

### 先写测试

**新功能与缺陷修复都先写测试，看着它失败，再写实现让它通过。**

- **修缺陷：先写一个能复现它的测试。** 测试在修复前必须是红的——否则它测的不是这个缺陷。
  本项目每一个真实缺陷都配了守护它的测试，见 [`docs/verification.md`](docs/verification.md#真实缺陷)
- **测行为，不测实现。** 断言"订阅之后收到了读数""CRC 被翻转的帧被丢弃并计数"，
  而不是"某个私有方法被调用了一次"
- **尽量不 mock 自己的代码。** 用真实的运行时、真实的网关应用、真实的拼帧逻辑；
  只有物理资源（串口、云存储、模型服务）用替身。本项目的替身都在 `tests/` 里，照着用
- **测试不得碰到真实凭证或公网。** `tests/conftest.py` 有自动兜底；新增碰外部资源的模块时，把它加进兜底列表
- **规则写成测试，而不是写在文档里。** 分层依赖、固件与上位机的命令码一致性，都是用测试读源码核对的
  （`tests/architecture/`、`tests/application/test_command_code_contract.py`）。
  只写在文档里的规则迟早会漂移——本仓库的依赖规则测试设立当天就查出了一处违规

### 手动的原子提交

- **一个提交只做一件事。** 重构与行为变更分开提交；格式化与逻辑修改分开提交
- **每个提交都能单独通过三项检查。** 不提交"先坏着、下个提交再修"的中间状态
- **逐个文件 `git add`，提交前看一遍 `git diff --staged`。** 不用 `git add -A` 把不认识的文件一起带进去
- **不提交生成物与本机文件**：`__pycache__`、构建产物、`data/` 下的运行期数据、`oss_config.json`（装着凭证）
- 提交信息用 `类型(范围): 说明`，说明写"为什么"而不只是"改了什么"：

```
fix(frame_stream): 半个帧头等待不再计为重同步

缓冲区末尾只剩帧头的第一个字节时必须保留等待，这是字节流的正常状态。
原先在这个分支上无条件计数，等待期间每次轮询都会再计一次，
一次实验因此报出 13 次同步错误而实际丢帧为 0。
```

常用类型：`feat`、`fix`、`refactor`、`test`、`docs`、`chore`。

## 架构约束

分层与依赖规则见 [`docs/architecture.md`](docs/architecture.md#依赖规则)，由 `tests/architecture/test_layering.py` 强制。要点：

- `ui` 与 `gateway` 只经 `api` 访问系统，互不 import；`gateway` 不依赖 PyQt6
- 具体的通道、模型客户端、存储只在 `application` 或 `scripts/` 的组合代码里创建
- `service` 需要外部能力时，在自己一侧定义协议（端口），不 import 适配层
- 一个文件只承担一个职责；界面代码里不出现串口、网络、协议解析

**改动分层之前先写清楚理由**：动机、影响范围、考虑过的替代方案。写在 PR 描述里，
重要的决定补一篇 [`docs/decisions/`](docs/decisions/README.md)。修改白名单要连同理由一起改。

## 编码规范

### 类型

- 公开函数与方法的参数、返回值都要标注，`mypy src` 必须零问题
- 模块首行 `from __future__ import annotations`；写 `X | None`、`list[str]`，不写 `Optional`、`List`
- **跨层传递的结构一律用 `@dataclass(frozen=True)`**，不用裸 `dict` / `tuple`。
  例如问答的事实对象逐个字段显式声明：出口的接地检查要枚举这些字段，`dict[str, Any]` 会同时废掉 `mypy` 和那道检查

### 异常与错误

- 不写裸 `except:`，不捕获了什么都不做
- 边界处转译异常，不让底层库的异常穿透：`communication` 把 pyserial 的异常转成自己的异常族，
  `api` 把下层异常转成 `ApiError` 族
- **在数据管线上运行的组件"计数不抛"**：派发器、历史记录器、网关的事件桥失败时只记计数与最后一次错误，
  不把异常抛回采集线程——一次屏幕刷新失败不值得把整个轮询循环带走。计数要能被查询到，失败不能是静默的
- `src/` 里不用 `print`；需要记录时用 `logging`

### 命名

PEP 8：模块与函数 `snake_case`，类 `PascalCase`，常量 `UPPER_SNAKE_CASE`。不用 `mgr`、`tmp` 这类缩写。

### 注释

- docstring 说清"做什么"与关键约束，不写长篇
- **注释写"为什么"，尤其是那些看起来多余、删掉也能跑的代码。** 例如：
  为什么派发器要把"记录"和"下发"分开（在回调里下发会重入串口）；
  为什么负数读数不下发给屏幕（固件侧字段无符号，会被画成一个很大的正数）。
  写清这一层，后来的人才不会顺手把一道防线简化掉
- 协议编解码里不直观的字节序、偏移量，注明依据

## 文档

- 新增或改变行为的功能，同一个 PR 里更新对应文档：用法进 `docs/getting-started.md`，
  结构进 `docs/architecture.md`，协议进 `docs/protocol.md`，取舍进 `docs/decisions/`
- 文档里的数字要能复现，写明从哪里来、怎么重算
- 已知的缺陷与局限照实写（例：[`docs/protocol.md`](docs/protocol.md#已知缺陷长度字段被破坏)）

## 使用 AI 编程助手

欢迎用 AI 编程助手，但**AI 生成的代码与人写的代码适用同一套标准，提交者对每一行负责。**

### 审查清单

合并 AI 生成的改动之前，逐条确认：

- [ ] **测试是先有的，而且在实现之前确实是失败的。** AI 很容易写出"实现与测试一起生成、天然通过"的测试，
      这种测试不证明任何事。至少手动把实现改坏一次，看测试是否变红
- [ ] **没有删除、跳过或放宽任何已有的测试与检查**（`skip`、`xfail`、调低断言、`# type: ignore`、`# noqa`、
      修改 `pyproject.toml` 里的检查配置）。需要改的话，单独提交并写明理由
- [ ] **没有越层。** 依赖规则测试会拦住 import，但拦不住"把业务逻辑写进界面回调"这类问题，要人看
- [ ] **没有顺手扩大改动范围**：任务之外的重构、重命名、格式调整另开提交
- [ ] **没有新增依赖**，或者新增的依赖有单独说明
- [ ] **文档与注释里的事实核对过。** AI 会写出看起来合理但不存在的函数名、文件路径和数字。
      引用的每个路径、每个数字都要能在仓库里找到出处
- [ ] **没有凭证、本机路径、个人信息**进入代码或提交

### 边界

- **提交与推送由人来做。** 不让 AI 工具自动执行 `git commit`、`git push` 或任何改写历史的操作
- **不把凭证交给 AI 工具**：不贴 `oss_config.json` 的内容，不让它读取
- **有破坏性的操作先确认**：删除文件、批量替换、改写迁移脚本，先让它列出将要改动的内容
- **约束写进测试，而不是只写在提示词里。** 给 AI 的指令会被遗忘或误读；
  会让一次违规变成一次失败的测试，才是可靠的约束

### 为什么这样要求

这个项目自己的问答功能遵循同一个原则：语言模型负责组织语言，数字和动作由代码负责，
模型的输出过一道确定性的检查才能生效（见 [`docs/decisions/02-llm.md`](docs/decisions/02-llm.md)）。
对 AI 编程助手也是一样：**它可以提议，决定合并的是测试与审查。**
